apply plugin: 'com.android.application'

android {
    compileSdkVersion 28

    defaultConfig {
        applicationId "me.tom.jsbridgewebview.sample"
        minSdkVersion 16
        targetSdkVersion 28
        versionCode 1
        versionName "1.3.2"
    }
}


dependencies {
    implementation 'androidx.appcompat:appcompat:1.1.0'
    implementation project(':library')
}